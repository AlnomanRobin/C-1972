#include <stdio.h>
int main()//take two number and his summation
{
    int A,B;
    printf("the first number:");
    scanf("%d",&A);
    printf("the second number:");
    scanf("%d",&B);
    int sum;
    sum=A+B;
    printf("The sum is:%d",sum);
    return 0;
}
